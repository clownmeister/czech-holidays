yarnPath: .yarn/releases/yarn-4.2.2.cjs

changesetBaseRefs:
  - HEAD

nodeLinker: node-modules
npmPublishRegistry: 'https://registry.npmjs.org'

npmScopes:
  clownmeister:
    npmAlwaysAuth: true
    npmAuthToken: '${secrets.NPM_TOKEN:secret}'
    npmRegistryServer: 'https://registry.npmjs.org'
